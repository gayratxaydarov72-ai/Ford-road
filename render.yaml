services:
  - type: web
    name: foldcraft-studio
    env: python
    buildCommand: "npm install && npm run build && pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PORT
        value: 10000
